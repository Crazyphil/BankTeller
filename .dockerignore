# Build artifacts are excluded from the context, except the pre-built fat JAR
# that the copy-only Dockerfile packages into the runtime image.
build
**/build
!server/build
!server/build/libs
!server/build/libs/server-all.jar
.gradle
**/.gradle
kotlin-js-store/.cache

# VCS and IDE
.git
.gitignore
.idea
.vscode
*.iml

# Local env / runtime
.env
*.log
node_modules
**/node_modules

# Manual test artifacts
openspec/changes/onboarding-enable-banking/manual-test-plan.md
