## 1. Core mechanism

- [x] 1.1 Create `ActionButton` composable in `app/web/src/commonMain/kotlin/it/kapfer/bankteller/ui/components/ActionButton.kt`: wraps Material `Button`, `enabled = !LocalActionBusy.current` baked in, KDoc documenting the global "one action at a time" semantic and the convention that all server-triggering buttons use it.
- [x] 1.2 Create `LocalActionBusy` composition local (`Boolean`, default `false`) alongside `ActionButton` (or in the same file).
- [x] 1.3 Provide `LocalActionBusy` in `App.kt` as `viewModel.isLoading || viewModel.linkStatusChecking` at the app shell level so all screens observe it.
- [x] 1.4 Update `DESIGN-LANGUAGE.md` with §8.5 "Processing Feedback & Wait States": the four-tier wait-state taxonomy (cold-start splash / action in-flight / screen transition / ambient polling) plus the in-flight rules (fixed button footprint, brass-only progress indicators, no skeleton shimmer on financial amounts). **Also codify the button conventions**: `ActionButton` and `QuietActionButton` as the standard components for server-action buttons; the `enabled` parameter for domain-specific conditions ANDed with the busy state; the rule that quiet-styled server-action buttons use `QuietActionButton` (not raw `QuietButton` with manual `enabled`); and the distinction between server-action buttons (disable during in-flight) and navigation-only controls (always enabled).
- [x] 1.5 Add `enabled: Boolean = true` parameter to `ActionButton`; effective enabled = `enabled && !LocalActionBusy.current`. Update KDoc to document the parameter and its use for domain-specific conditions (e.g. "Continue with <bank>" requires `selectedPsuType.isNotEmpty()`).
- [x] 1.6 Create `QuietActionButton` composable in the same file: same busy semantics as `ActionButton` (`enabled && !LocalActionBusy.current`), quiet visual treatment matching `QuietButton`. For quiet-styled server-action buttons (e.g. "Re-open linking page") that need busy disabling without changing visual hierarchy.

## 2. Migration of existing buttons

- [x] 2.1 OnboardingScreen.kt — replace raw `Button` for "Send login email" (:175), "Register" (:358), "Start Bank Setup" (:546), "Connect <bank>" (:749), "Retry" (:780), "Retry" (:1022) with `ActionButton`.
- [x] 2.2 OnboardingScreen.kt — migrate the two hand-rolled sites (:1002 `!linkStatusChecking && !isLoading`, :1064 `!isLoading`) to `ActionButton`, removing the manual `enabled` expressions.
- [x] 2.3 LoginScreen.kt — replace the conditional spinner toggle (`if (isLoading) CircularProgressIndicator() else Button(...)`) with a stable `ActionButton` so the form does not jump during submission (D6 footprint rule). Scan `DashboardScreen.kt`, `CallbackScreen.kt` for server-action buttons; migrate any found to `ActionButton` (leave navigation-only buttons untouched).
- [x] 2.4 Migrate "Continue with <bank>" (OnboardingScreen.kt:794) from hand-rolled `enabled = selectedPsuType.isNotEmpty() && !isLoading` to `ActionButton(enabled = selectedPsuType.isNotEmpty())` — the domain condition goes through the `enabled` param, the busy state is automatic.
- [x] 2.5 Migrate "Re-open linking page" (OnboardingScreen.kt:1261) from `QuietButton(enabled = !isLoading)` to `QuietActionButton` — same quiet visual, busy state automatic.
- [x] 2.6 Migrate navigate-and-fire controls that trigger server-side requests to `ActionButton`/`QuietActionButton`: "Restart onboarding" (POST reset), "Back" on LinkingProgress (POST cancel-linking), and any other button that both navigates and fires a server call. Pure client-side navigation (step Back/Cancel without a server call) stays as-is.

## 3. Cold-start routing screen

- [x] 3.1 Add `Screen.Loading` to the `Screen` enum; change `AppViewModel.currentScreen` initial value from `Screen.Login` to `Screen.Loading` (AppViewModel.kt:68). Verify every `checkAuth()`/`checkOnboardingStatus()` branch still sets a concrete screen (Login/Dashboard/Onboarding) so `Loading` is transient.
- [x] 3.2 Create `LoadingSplash` composable in `it.kapfer.bankteller.ui.components` (48dp `BrandLogo`, Fraunces "BankTeller" headline, 24dp brass `CircularProgressIndicator` stroke 2.5dp, centered in `ScreenShell(maxWidth = Dimens.formMaxWidth)`, wrapped in `ThemeToggleOverlay`, no entrance animation) and wire to `Screen.Loading` in `App.kt`'s `when (viewModel.currentScreen)`.
- [x] 3.3 Replace the plain text `<div id="loading">Loading BankTeller…</div>` in `app/web/index.html` with a branded HTML splash matching the Compose `LoadingSplash` visual: inline SVG `BrandLogo` (light + dark variants via `prefers-color-scheme`), Fraunces wordmark (with `@font-face` on the preloaded woff2 + serif fallback), 24px brass CSS spinner (2.5px border, 0.8s linear), theme-aware background/text/brass via CSS variables. This shows before Compose mounts; the transition to the Compose splash must be visually seamless.
- [x] 3.4 Add inline `<script>` in `app/web/index.html` `<head>` that reads `localStorage["bankteller-theme"]` and sets the CSS variables (`--bg`, `--text`, `--brass`) before first paint, so users with a manual theme override don't see a theme flash on cold start. Falls back to `prefers-color-scheme` when no override is stored. (D8.)
- [x] 3.5 Confirm the post-login flow is unchanged: after explicit login submission the login form stays visible (busy-disabled via `ActionButton`) until routing completes — no `Screen.Loading` interposed after login.

## 4. ViewModel fixes

- [x] 4.1 Set `isLoading = true` around `loadAspsps()` in `AppViewModel.kt` so the ASPSP catalog fetch disables other action buttons while in flight. Reset in the completion/error path.
- [x] 4.2 Set `isLoading = true` around `submitRegistration()` in `AppViewModel.kt` so the registration request disables other action buttons while in flight. Reset in the completion/error path.

## 5. Verification

- [x] 5.1 Run `./gradlew :app:web:containerJsBrowserTest` — all suites green, no new failures. (Re-run after the section-6 migration; the previous green run predates the WaitingIndicator changes.)
- [x] 5.2 Manual: on "Choose your bank", click Connect and verify the button disables during the request and re-enables after; verify Back/Cancel stay clickable throughout.
- [x] 5.3 Manual: cold-start the app with a valid session (hard refresh) — verify the loading splash appears immediately and the app transitions directly to Dashboard/Onboarding without the login screen flashing. Then cold-start without a session — verify the splash transitions to the login screen.
- [x] 5.4 Manual: set a manual theme override (toggle theme in the app), then hard refresh — verify the HTML splash and Compose splash both honor the persisted override with no theme flash.
- [x] 5.5 Manual: verify wait-indicator consistency — the "Verifying" step and "Check your email" step show a centered 32dp spinner (no longer left-aligned), matching the bank-list loading state; the link-status check and CallbackScreen redirect show the compact 16dp inline spinner; the AuthProgress "Continue to your bank" button stays mounted (disabled) during `POST /api/auth` instead of being replaced by a spinner.

## 6. Standardized wait indicators (D9)

- [x] 6.1 Create `WaitingIndicator` in `app/web/src/commonMain/kotlin/it/kapfer/bankteller/ui/components/WaitingIndicator.kt`: object with two composables — `Zone(modifier: Modifier = Modifier, verticalPadding: Dp = Dimens.xxl)` rendering a `Box(fillMaxWidth, contentAlignment = Center)` with `padding(vertical = verticalPadding)` around a 32dp (`Dimens.xl`) brass `CircularProgressIndicator` with 2.5dp stroke; and `Inline(modifier: Modifier = Modifier)` rendering a 16dp (`Dimens.md`) brass `CircularProgressIndicator` with 2.0dp stroke. Color always `LocalBankTellerColors.current.brass`; size/stroke/color not parameterizable. KDoc documents the surface rules (content-zone vs. inline) and the convention that all standalone wait indicators use this component.
- [x] 6.2 OnboardingScreen.kt — migrate the four content-zone spinner sites to `WaitingIndicator.Zone()`: WaitingStep (:250), VerifyingStep (:503), BankSelection resume-card loading (:758), BankSelection bank-list loading (:981). Remove the hand-rolled `Box`+`Center`+padding wrappers at :758/:981 and the bare spinners at :250/:503.
- [x] 6.3 Migrate the two inline spinner sites to `WaitingIndicator.Inline()`: link-status checking on LinkingProgress (OnboardingScreen.kt:1242) and the redirect wait in CallbackScreen.kt:217.
- [x] 6.4 Fix the AuthProgress footprint violation (OnboardingScreen.kt:1290-1305): replace the `if (authRedirectUrl != null) Button(...) else CircularProgressIndicator(...)` branch with a stable `ActionButton` ("Continue to your bank") that stays mounted and disables via `LocalActionBusy` while `POST /api/auth` is in flight (D6 rule; same fix pattern as task 2.3 applied to LoginScreen).
- [x] 6.5 Amend `DESIGN-LANGUAGE.md` §8.5: add §8.5.1 "Progress Indicator Surfaces & Specifications" — the surface table (button: no spinner, busy state inside `ActionButton`/`QuietActionButton`; content-zone: `WaitingIndicator.Zone`, 32dp/2.5dp, centered, 48dp vertical padding; inline: `WaitingIndicator.Inline`, 16dp/2.0dp; cold-start splash: 24dp/2.5dp, restricted to `LoadingSplash`), the thin-stroke rationale (Private Ledger hairline aesthetic, ~1:13 ring-to-stroke ratio vs. Material's 4dp default), and a sentence in the tier taxonomy naming the content-zone wait case: waits with no visible trigger button and no inline status context render as content-zone waits via `WaitingIndicator.Zone`.
