## 1. Control-plane refreshToken parsing + persistence

- [ ] 1.1 In `EnableBankingControlPlaneClient.kt`, extend `EmailLinkSigninResult.Ok` to include a `refreshToken: String` field (parsed from the GIT `emailLinkSignin` JSON response's `refreshToken` field)
- [ ] 1.2 Update the `emailLinkSignin` response parser to populate `refreshToken` alongside the existing `idToken`
- [ ] 1.3 Update the email-link callback route's `emailLinkSignin` handler to cache both `idToken` AND `refreshToken` on the onboarding context (spec MODIFIED: Email-link callback route)
- [ ] 1.4 Update `OnboardingService.completeOnboarding` to persist the `refreshToken` from the onboarding context into `system_config` as `enable_banking_refresh_token` (spec MODIFIED: Onboarding completion endpoint, step 6)
- [ ] 1.5 Update `OnboardingService.resetCredentials` to also blank `enable_banking_refresh_token` (spec MODIFIED: Onboarding credentials reset endpoint)
- [ ] 1.6 Unit test: `emailLinkSignin` parses refreshToken from a mocked GIT response
- [ ] 1.7 Unit test: `completeOnboarding` persists all three keys (`application_id`, `private_key`, `refresh_token`) on success
- [ ] 1.8 Unit test: `resetCredentials` blanks all four keys including `enable_banking_refresh_token`

## 2. Control-plane idToken refresh

- [ ] 2.1 In `EnableBankingControlPlaneClient.kt`, add a sealed `IdTokenRefreshResult` type: `Ok(idToken, refreshToken, expiresIn)`, `Error(statusCode, message)` (spec ADDED: Control-plane idToken refresh). No `InvalidRefreshToken` variant — failures are surfaced as `Error` and the caller stops (no fallback re-auth).
- [ ] 2.2 Implement `refreshIdToken(refreshToken: String): IdTokenRefreshResult` — calls `POST https://securetoken.googleapis.com/v1/token?key=<FIREBASE_API_KEY>` with form body `grant_type=refresh_token&refresh_token=<token>`, parses `id_token`/`refresh_token`/`expires_in`/`user_id`, returns `Error` on any non-200 response. **Owns the full token lifecycle**: on success, persists the returned `refresh_token` to `system_config` as `enable_banking_refresh_token` (token may be rotated by Google); on failure (invalid/expired/revoked — non-200), clears `enable_banking_refresh_token` from `system_config` so the next onboarding-state check routes to re-login. Transient errors (network, 5xx) do NOT clear the token.
- [ ] 2.3 Reuse the existing `FIREBASE_API_KEY` constant already used by `emailLinkSignin`
- [ ] 2.4 Unit test: `refreshIdToken` parses a successful response, returns `Ok` with all fields, and persists the new `refresh_token` to `system_config`
- [ ] 2.5 Unit test: `refreshIdToken` returns `Error` on a non-200 response (invalid/expired/revoked) and clears `enable_banking_refresh_token` from `system_config`
- [ ] 2.6 Unit test: `refreshIdToken` returns `Error` on a transient failure (network/5xx) and does NOT clear `enable_banking_refresh_token` from `system_config`

## 3. Data-plane ASPSP listing endpoint

- [ ] 3.1 In `EnableBankingClient.kt`, implement `getAspsps(): AspsspListResult` — calls `GET /aspsps` with **no query parameters** (all ASPSPs fetched, no server-side filtering), RS256 JWT auth (spec ADDED: Enable Banking data-plane ASPSP listing)
- [ ] 3.2 Add `AspsspListResult` sealed type: `Ok(aspsps: List<Aspssp>)`, `Error(statusCode, message)`; add `Aspssp` data class with `name`, `country`, `bic`, `logo`, `psuTypes`, `maximumConsentValidity` (seconds — used later to compute the max `valid_until` for `POST /auth`). Banks are identified by the `{name, country}` pair — the Enable Banking API has no ASPSP UID.
- [ ] 3.3 Add server route `GET /api/aspsps` in `OnboardingRoutes.kt` (or a new `AccountLinkingRoutes.kt`) — authenticated (BankTeller session cookie), calls `EnableBankingClient.getAspsps`, returns the full JSON array **unfiltered** (spec ADDED: Bank-listing endpoint)
- [ ] 3.4 Unit test: `getAspsps` sends RS256 JWT auth and no query params
- [ ] 3.5 Unit test: `GET /api/aspsps` returns 401 without session cookie
- [ ] 3.6 Unit test: `GET /api/aspsps` returns 200 with the full bank list JSON, including `maximum_consent_validity`

## 4. Data-plane auth + session-creation endpoints

- [ ] 4.1 In `EnableBankingClient.kt`, implement `startAuth(aspspName: String, aspspCountry: String, psuType: String, access: Access, state: String, redirectUrl: String): StartAuthResult` — calls `POST /auth` with JSON body (`access`, `aspsp` object `{name, country}`, `state`, `redirect_url`, `psu_type`), RS256 JWT auth, returns `url`, `authorization_id`, `psu_id_hash` (spec ADDED: Enable Banking data-plane auth initiation). The `access` is constructed with `balances=true`, `transactions=true`, and `validUntil = now + aspsp.maximumConsentValidity` (max allowed).
- [ ] 4.2 Add `StartAuthResult` sealed type: `Ok(url, authorizationId, psuIdHash)`, `Error(statusCode, message)`; add `Access` data class with `validUntil` (RFC3339), `balances` (default true), `transactions` (default true)
- [ ] 4.3 In `EnableBankingClient.kt`, implement `authorizeSession(code: String): AuthorizeSessionResult` — calls `POST /sessions` with JSON body `{ "code": <code> }`, RS256 JWT auth, returns `session_id`, `accounts[]`, `aspsp`, `psu_type`, `access` (spec ADDED: Enable Banking data-plane session authorization)
- [ ] 4.4 Add `AuthorizeSessionResult` sealed type: `Ok(sessionId, accounts, aspsp, psuType, access)`, `Error(statusCode, message)`. No `getSession`/status-enum method — a 200 response from `POST /sessions` is the sole success indicator (the response has no `status` field); a non-200 response is an error.
- [ ] 4.5 Unit test: `startAuth` sends correct JSON body and RS256 JWT auth, with `validUntil` set to `now + maximumConsentValidity`
- [ ] 4.6 Unit test: `authorizeSession` sends `code` and parses session response

## 5. Control-plane account-linking endpoint

- [ ] 5.1 In `EnableBankingControlPlaneClient.kt`, implement `linkAccounts(applicationId: String, country: String, psuType: String, aspspName: String, idToken: String): LinkAccountsResult` — calls `POST /api/link_accounts` with `multipart/form-data` body (form fields, camelCase: `country`, `aspsp` (the bank name — the control-plane API identifies banks by name string, not UID), `appId`, `psuType`, `redirectUrl`), `Authorization: Bearer <idToken>` header, returns `authorization_url` and `psu_id_hash` (spec ADDED: Control-plane account linking). The `redirectUrl` is a **fixed code constant** `https://enablebanking.com/api/auth_redirect` (Enable Banking's own control-panel callback — external redirect URLs do not work). No `redirectUrl` parameter in the method signature — it's a constant inside the client.
- [ ] 5.2 Add `LinkAccountsResult` sealed type: `Ok(authorizationUrl, psuIdHash)`, `Error(statusCode, message)`. No `Unauthorized` variant and no 401 retry — the caller always supplies a fresh idToken obtained via `refreshIdToken` before calling.
- [ ] 5.3 Unit test: `linkAccounts` sends form-data with correct fields and Bearer idToken

## 6. Account-linking + auth server routes

- [ ] 6.1 Add `POST /api/link-accounts` route — authenticated (session cookie), reads `application_id`/`refreshToken` from `system_config`, calls `refreshIdToken` to obtain a fresh idToken, calls `linkAccounts` (the `redirectUrl` is a fixed constant inside the client, not read from `system_config`), stores the `psu_id_hash` **and the selected bank info** (`aspsp_name`, `aspsp_country`, `psu_type`) in the user session (needed by the state endpoint for the resume flow), returns `{ "authorization_url", "psu_id_hash" }` (spec ADDED: Account-linking endpoint)
- [ ] 6.2 Add `POST /api/auth` route — authenticated (session cookie), **clears any stored `auth_error` from the user session at the start of the request** (the user is starting a fresh attempt — the old error must not follow them into the new attempt or appear in a subsequently opened tab), generates a `state` JWT containing the session ID, reads `redirect_url` from `system_config` (`enable_banking_redirect_url`), computes `valid_until = now + aspsp.maximumConsentValidity` (looked up from the ASPSP list), calls `startAuth` with `access = { balances: true, transactions: true, valid_until }`, returns `{ "url", "authorization_id", "psu_id_hash" }` (spec ADDED: Auth initiation endpoint)
- [ ] 6.3 Extend the existing `GET /enable-banking-callback` route to handle auth returns: if `code` is present and `oobCode` is absent, **verify the session cookie** (same browser tab → cookie expected, unlike the email-link callback which may be a different device). On missing/invalid session cookie, **302 redirect to the BankTeller login page** (username/password login — not the Enable Banking email-link flow) and discard the `code` (single-use, time-limited). On valid session: decode `state` JWT to get session ID, call `authorizeSession(code)`. On success (200 — sole success indicator, no `status` field in the response), clear any stored `auth_error` from the user session (belt-and-suspenders — `POST /api/auth` already clears it), store `session_id` + `accounts[]` in the user session, serve SPA bundle. On failure (non-200 from `authorizeSession`, or invalid `state` JWT), **store the error reason as `auth_error` in the user session** (so the SPA can display it on the LinkingProgress screen with a "Continue to authorization" retry button via the `GET /api/onboarding/state` resume flow — the error is cleared when the user clicks the button, via `POST /api/auth`), serve SPA bundle (spec MODIFIED: Email-link callback route — auth callback handling).
- [ ] 6.4 Add `GET /api/onboarding/link-status` route — authenticated (session cookie), calls `GET /application` on the control-plane API (fresh idToken via `refreshIdToken`), inspects `whitelisted_accounts[]` for a `psu_id_hash` match (stored from the link-accounts response), returns `{ "linked": bool }` (spec ADDED: Link-completion check endpoint). **Single on-demand check** (triggered by a user-clicked button), not polling.
- [ ] 6.5 Add `GET /api/onboarding/state` route — authenticated (session cookie), inspects the **BankTeller user session** (ephemeral, tied to the session cookie) and `system_config` to return a structured response with **routing flags** and **display-only fields** (per D10): `{ "requires_relogin": bool, "linking_completed": bool, "auth_completed": bool, "auth_error": string|null, "selected_bank": object|null }` (spec ADDED: Onboarding state endpoint). `requires_relogin` = true if `enable_banking_refresh_token` is missing from `system_config` (sole re-login signal — the SPA never parses `auth_error` for routing). `linking_completed` = true if `psu_id_hash` is in the user session. `auth_completed` = true if `session_id` + `accounts[]` are in the user session. `auth_error` = stored error reason or null (display-only, ephemeral — dies with the session, cleared on new `POST /api/auth` attempt). `selected_bank` = `{ "aspsp_name", "aspsp_country", "psu_type" }` from the user session (stored by `POST /api/link-accounts`), or null if linking not completed — the SPA needs this to call `POST /api/auth` in the resume flow.
- [ ] 6.6 Unit test: `POST /api/link-accounts` returns 401 without session cookie, 200 with authorization_url on success
- [ ] 6.7 Unit test: `POST /api/auth` generates a valid state JWT, sets `valid_until` to `now + maximumConsentValidity`, and returns authorization url
- [ ] 6.7.1 Unit test: `POST /api/auth` clears any stored `auth_error` from the user session at the start of the request (so a subsequent `GET /api/onboarding/state` sees `auth_error: null` — verifies the error does not follow the user into the new attempt or appear in a subsequently opened tab)
- [ ] 6.8 Unit test: `GET /enable-banking-callback` with `code`+`state` (no `oobCode`) and a valid session cookie verifies the session, calls `authorizeSession`, stores `session_id` + `accounts[]` in the user session, clears any stored `auth_error` from the user session, and serves SPA
- [ ] 6.9 Unit test: `GET /enable-banking-callback` with `code`+`state` and **no session cookie** 302 redirects to the BankTeller login page (not 401, not the Enable Banking email-link flow)
- [ ] 6.10 Unit test: `GET /enable-banking-callback` with `code`+`state`, valid session cookie, but `authorizeSession` returns non-200 → stores the error reason as `auth_error` in the user session and serves SPA bundle
- [ ] 6.11 Unit test: `GET /enable-banking-callback` with `oobCode`+`state` still runs the email-link flow without a session cookie (no regression)
- [ ] 6.12 Unit test: `GET /api/onboarding/link-status` returns `linked: true` when `psu_id_hash` is found in `whitelisted_accounts`, `linked: false` otherwise
- [ ] 6.13 Unit test: `GET /api/onboarding/state` returns correct `(requires_relogin, linking_completed, auth_completed, auth_error, selected_bank)` for each state: no progress (all false/null), linking done (linking_completed=true, selected_bank populated), linking done + auth error (auth_error set, selected_bank populated), both done (auth_completed=true), refresh token missing (requires_relogin=true)
- [ ] 6.14 Unit test: `GET /api/onboarding/state` returns `requires_relogin: true` (not `auth_error: "refresh_token_missing"`) when `enable_banking_refresh_token` is missing from `system_config` — verifies the SPA routes via the flag, not string parsing

## 7. SPA — ActivationGuide update + onboarding-state routing

- [ ] 7.1 Update the `ActivationGuide` step content to explain the **two-step process**: the user will authorize with their bank **twice** — first to link their account (in a new browser tab), then to create a session (in the current tab). Both are required to provide free access to their accounts. Primary button reads "Start Bank Setup" (spec MODIFIED: SPA ActivationGuide step)
- [ ] 7.2 Visual: reuse the existing `Card` container, `headlineSmall` header, and `bodyMedium` body from `OnboardingScreen.kt`; secondary `TextButton` "Logout" stays (delegate visual polish to @designer per design.md UI Design section)
- [ ] 7.3 Add `GET /api/onboarding/state` call to `AppViewModel` on SPA load — the SPA routes based on the **routing flags** (never parses `auth_error` strings): `requires_relogin=true` → re-login; `(false, false, false)` → ActivationGuide (fresh start); `(false, true, false)` → LinkingProgress with "Continue to authorization" button (resume after new tab or cancelled auth); `(false, true, false)` + `auth_error!=null` → LinkingProgress with error message + retry button (resume after auth failure); `(false, true, true)` → dashboard. When routing to LinkingProgress, the SPA stores `selected_bank` from the response for the `POST /api/auth` call (spec ADDED: Onboarding state endpoint)

## 8. SPA — BankSelection step

- [ ] 8.1 Add `BankSelection` screen component to the onboarding flow — fetches the full bank list from `GET /api/aspsps` and filters **client-side** (by search text, country, PSU type) (spec ADDED: SPA BankSelection step)
- [ ] 8.2 Add `GET /api/aspsps` call to `AppViewModel` — fetches the full bank list once, exposes `aspspsState: AspspsState` (loading/loaded/error); filtering is done in the viewmodel/view, not on the server
- [ ] 8.3 On bank selection, collect `aspsp_name`, `aspsp_country`, `psu_type` and advance to `LinkingProgress` — trigger `POST /api/link-accounts` via `AppViewModel`
- [ ] 8.4 Visual: `OutlinedTextField` for search, `LazyColumn` of bank items (country badge, bank name bold, BIC in `robotoMonoFamily`, `FilterChip` for PSU type), `CircularProgressIndicator` during load — per design.md UI Design section (delegate to @designer)
- [ ] 8.5 Handle API error states (loading, empty list, network error) with styled error states consistent with existing onboarding screens

## 9. SPA — LinkingProgress step

- [ ] 9.1 Add `LinkingProgress` screen component — after `POST /api/link-accounts` returns `authorization_url` and `psu_id_hash`, the SPA opens `authorization_url` in a **new browser tab** (EB control panel) and shows an explanatory message in the original tab (spec ADDED: SPA LinkingProgress step)
- [ ] 9.2 Store `psu_id_hash` in viewmodel state for the later link-completion check
- [ ] 9.3 Show a message explaining that the user should complete linking in the other tab, then click "I've completed linking, authorize now". **No background polling.** When the button is clicked, the SPA calls `GET /api/onboarding/link-status` **once** (spec MODIFIED: Link-completion check — single on-demand check)
- [ ] 9.4 On `linked: true`, transition to `AuthProgress`. On `linked: false`, show an error and let the user retry the check or re-initiate linking.
- [ ] 9.5 **Resume mode**: when the SPA loads via `GET /api/onboarding/state` with `(requires_relogin=false, linking_completed=true, auth_completed=false)`, show this screen with a "Continue to authorization" button. If `auth_error` is present, show the error message (e.g. "Bank denied the authorization", "Invalid or expired code", "Technical error") above the button. The button triggers `POST /api/auth` to start a fresh authorization, passing the `selected_bank` fields (`aspsp_name`, `aspsp_country`, `psu_type`) from the state endpoint response as the request body — the SPA doesn't have them in viewmodel state after a reload. The server clears `auth_error` from the user session at the start of that request, so the error is shown exactly once and does not follow the user into the new attempt or appear in a subsequently opened tab (spec ADDED: SPA LinkingProgress step — resume scenarios).
- [ ] 9.6 Visual: `Card` with `headlineSmall` header, `bodyMedium` message, primary `Button` ("I've completed linking, authorize now" / "Continue to authorization" — label adapts to context), error `Text` on failure — per design.md UI Design section (delegate to @designer)

## 10. SPA — AuthProgress step

- [ ] 10.1 Add `AuthProgress` screen component — after `POST /api/auth` returns `authorization_url`, the SPA redirects the **current tab** (not a new tab) to `authorization_url` (bank SCA for session) (spec ADDED: SPA AuthProgress step)
- [ ] 10.2 On return from the bank (`GET /enable-banking-callback` with `code`+`state`, processed server-side), the SPA reads the callback result — if session authorization succeeded, forward to the **existing dashboard** (unchanged, out of scope)
- [ ] 10.3 On callback errors (invalid code, state mismatch, authorization failure), the SPA loads, calls `GET /api/onboarding/state`, sees `(requires_relogin=false, linking_completed=true, auth_completed=false, auth_error="<reason>", selected_bank={...})`, and shows the LinkingProgress screen with the error message and a "Continue to authorization" retry button (using `selected_bank` for the `POST /api/auth` call). The missing-session-cookie case is handled server-side by a 302 redirect to login — the SPA does not render an error screen for it.
- [ ] 10.4 Visual: `Card` with `headlineSmall` header and `bodyMedium` message explaining the redirect; the redirect happens automatically on entering this step — per design.md UI Design section (delegate to @designer)

## 11. Integration + manual testing

- [ ] 11.1 End-to-end manual test: complete onboarding → registration → ActivationGuide (two-step explanation) → bank selection → linking (new tab) → "I've completed linking" button → link-status check → auth (current tab) → callback → dashboard (using a test/sandbox bank from EB)
- [ ] 11.2 Manual test: link-completion check — verify `GET /application` shows `whitelisted_accounts[].identification_hash` matching `psu_id_hash` after bank SCA, and that the single `GET /api/onboarding/link-status` call returns `linked: true`
- [ ] 11.3 Manual test: auth callback — verify `GET /enable-banking-callback` with `code`+`state` verifies the session cookie, creates a session, and forwards to the dashboard
- [ ] 11.4 Manual test: callback regression — verify `GET /enable-banking-callback` with `oobCode`+`state` still works for the email-link flow (no session cookie required)
- [ ] 11.5 Manual test: auth callback without session cookie — verify a **302 redirect to the BankTeller login page** (not 401), and that after re-login the SPA resumes via `GET /api/onboarding/state` to the LinkingProgress screen with a "Continue to authorization" button
- [ ] 11.6 Manual test: resume after new tab — open a new tab while the original is at the bank's SCA page; verify the new tab loads the SPA, calls `GET /api/onboarding/state`, and shows the LinkingProgress screen with a "Continue to authorization" button
- [ ] 11.7 Manual test: resume after auth failure — trigger an auth callback failure (e.g. invalid code); verify the SPA loads, calls `GET /api/onboarding/state`, and shows the LinkingProgress screen with the error message and a "Continue to authorization" retry button
- [ ] 11.7.1 Manual test: auth_error cleared on new attempt — after an auth callback failure, click "Continue to authorization" (triggering `POST /api/auth`), then open a new tab while at the bank's SCA page; verify the new tab calls `GET /api/onboarding/state` and sees `auth_error: null` (the old error was cleared by `POST /api/auth` and does not follow the user into the new attempt)
- [ ] 11.8 Manual test: refresh-token invalid — simulate an invalid/expired refresh token; verify `refreshIdToken` clears `enable_banking_refresh_token` from `system_config`, and the next `GET /api/onboarding/state` returns `requires_relogin: true` so the SPA routes to re-login
